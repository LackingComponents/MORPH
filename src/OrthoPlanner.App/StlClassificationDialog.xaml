<Window x:Class="OrthoPlanner.App.StlClassificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Classify Dental Scans" Width="520" Height="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgDarkBrush}" Foreground="{StaticResource TextPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🦷" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Classify Loaded Scans" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Assign each dental scan as Upper, Lower, or Other."
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- File list -->
        <Border Grid.Row="1" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="FileList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,10" Margin="4" CornerRadius="4" Background="{StaticResource BgLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding FileName}" FontSize="13" FontWeight="Medium"
                                               VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0">
                                        <RadioButton Content="Upper" GroupName="{Binding GroupKey}" Tag="Upper"
                                                     IsChecked="{Binding IsUpper, Mode=TwoWay}" Foreground="#FF8AB4F8" Margin="0,0,12,0"
                                                     VerticalAlignment="Center" FontWeight="Medium"/>
                                        <RadioButton Content="Lower" GroupName="{Binding GroupKey}" Tag="Lower"
                                                     IsChecked="{Binding IsLower, Mode=TwoWay}" Foreground="#FFF472B6" Margin="0,0,12,0"
                                                     VerticalAlignment="Center" FontWeight="Medium"/>
                                        <RadioButton Content="Other" GroupName="{Binding GroupKey}" Tag="Other"
                                                     IsChecked="{Binding IsOther, Mode=TwoWay}" Foreground="{StaticResource TextSecondaryBrush}"
                                                     VerticalAlignment="Center" FontWeight="Medium"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="90" Padding="10,6" Click="Cancel_Click" Margin="0,0,10,0"/>
            <Button x:Name="OkButton" Style="{StaticResource AccentButton}" Content="OK" Width="90" Padding="10,6" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
