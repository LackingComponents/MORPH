<Window x:Class="OrthoPlanner.App.Views.DicomSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        xmlns:vms="clr-namespace:OrthoPlanner.App.ViewModels"
        Title="Select DICOM Series" Height="450" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Foreground="#E0E0E0"
        d:DataContext="{d:DesignInstance Type=vms:DicomSelectorViewModel}">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="#333333" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="15,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Border Background="#FF0C1018" CornerRadius="8" BorderBrush="#333333" BorderThickness="1" Margin="10">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Multiple Series Found:" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />

            <ListBox Grid.Row="1" ItemsSource="{Binding SeriesList}" SelectedItem="{Binding SelectedSeries}"
                     Background="Transparent" BorderThickness="0" Margin="0,0,0,15"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#1A1F2B" CornerRadius="4" Padding="10" Margin="0,0,0,5" Width="520">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- Axial Preview -->
                            <Image Source="{Binding PreviewImage}" Width="80" Height="80"
                                   Stretch="Uniform" HorizontalAlignment="Left" />

                            <!-- Metadata -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Bold" Foreground="White" />
                                <TextBlock Text="{Binding PatientDetails}" Foreground="#BBBBBB" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding StudyDetails}" Foreground="#BBBBBB" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,5" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#2A354A" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource DialogButton}" />
                <Button Content="Load Series" Command="{Binding AcceptCommand}" Style="{StaticResource DialogButton}" Background="#007ACC" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
